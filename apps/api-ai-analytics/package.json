{
  "name": "api-ai-analytics",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 4005",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 4005",
    "test": "pytest"
  }
}
